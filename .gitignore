# macOS metadata
.DS_Store

# Xcode user data and derived data
DerivedData/
build/
HiFidelity.xcodeproj/xcuserdata/
HiFidelity.xcodeproj/project.xcworkspace/xcuserdata/
*.xcuserstate

# Local build archives / dated build folders
HiFidelity *.xcarchive
HiFidelity 20*/

# Sparkle release artifacts (keep locally, publish separately)
releases/*.zip
releases/appcast-item-*.xml

# Exported app bundles (local builds)
Exports/

# Note: docs/HiFidelity-*.zip files ARE committed for GitHub Pages hosting

# Temporary files
*.tmp
*.temp
/tmp/

# Environment and configuration files with secrets
.env
.env.local
*.secrets
config.local.*
secrets.plist

# Code signing certificates and keys (NEVER commit these!)
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
*.keychain
*.keychain-db

# Xcode user-specific settings (may contain signing info)
*.xcuserstate
*.xcuserdatad
*.xcworkspace/xcuserdata/

# Log files (may contain sensitive information)
*.log
*.xcactivitylog
build/Logs/

# Local development files
local.*
*.local
.DS_Store

# Backup files
*.bak
*~
*.swp
*.swo

# IDE files
.idea/
.vscode/
*.code-workspace
